spring:
  application:
    name: ProductCatalog

  datasource:
    url: jdbc:postgresql://localhost:5431/ProductCatalog
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver


  jpa:
    show-sql: true

  flyway:
    enabled: true
    locations: classpath:db/migrations


